@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        NavManager.NavigateTo($"signin?returnurl={Uri.EscapeDataString(NavManager.Uri)}", forceLoad: true);
    }
}
